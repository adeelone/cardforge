# Contributing

1. Create a focused branch.
2. Run `npm run lint`, `npm run typecheck`, and `npm run test`.
3. Keep changes scoped and include tests for reducers, exporters, or persistence changes.
4. Open a pull request with screenshots for UI changes.

Use Conventional Commits for commit messages.
